# Security

Codex Hub is distributed as a source patch and local installer script. Do not publish your Codex config, auth files, session logs, npm package cache, or built binaries with this repository.

The HUD renders data already present in the running Codex TUI. It does not add a network client or telemetry sink.
